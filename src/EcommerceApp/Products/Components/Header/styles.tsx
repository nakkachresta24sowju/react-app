import styled from "@emotion/styled";
import tw from "tailwind.macro";

const HeaderContainer = styled.div`
  ${tw`p-5 h-20 flex justify-between`};
`;
const ProductsFound = styled.p`
  ${tw``};
`;

export { HeaderContainer, ProductsFound };
